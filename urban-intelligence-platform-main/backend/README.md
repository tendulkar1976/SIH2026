# Urban Intelligence Platform - Backend

This is the FastAPI backend for the Urban Intelligence Platform.

## Run Locally

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```
